1つの文字列を入力して、それを 10 進数とみたとき、それを 10 進数としての数値（整数）に変換し、出力するプログラムを作成せよ。このとき、繰り返し構文と配列を用いること。
（ただし出力の最後は改行し、余計な文字・空行・空白は含まないこと。）

<br>
★入力制限（範囲外の入力は想定しなくて大丈夫）
入力 1: 1 文字以上 6 文字以下の文字列
ただし、1文字目は "+"or "-" とし、2文字目以降は、"1~9" の数値であるとする。

<br>
★入出力例
|入力|出力|
|---|---|
|+123|123|
|-5987|-5987|
