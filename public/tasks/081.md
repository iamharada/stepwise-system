1つの文字列を入力して、その文字列中にアルファベットが含まれていた場合、それらを大文字ならば、小文字に変換し、出力するプログラムを作成せよ。このとき、繰り返し文を用いること。
（ただし出力の最後は改行し、余計な文字・空行・空白は含まないこと。）

<br>
★入力制限（範囲外の入力は想定しなくて大丈夫）
入力 1: 1 文字以上 20 文字以下の文字列（半角のアルファベット（大文字 or 小文字） or 半角の数字から構成される。）

<br>
★入出力例
|入力|出力|
|---|---|
|abc|ABC|
|abcDEfg|ABCdeFG|
